# ============================================
# ENVIRONMENT VARIABLES CONFIGURATION
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ===== SERVER CONFIGURATION =====
# Port for the server to listen on (defaults to 5000)
PORT=5000

# ===== AUTHENTICATION =====
# JWT Secret Key - REQUIRED for authentication
# Generate a strong random string (at least 32 characters)
# Example: openssl rand -base64 32
SESSION_SECRET=your-super-secret-jwt-key-change-this-in-production

# ===== DATABASE CONFIGURATION =====
# MongoDB Connection String
# Format: mongodb://username:password@host:port/database?options
# Or: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/finance-manager

# ===== FRONTEND CONFIGURATION (Optional) =====
# Only needed if frontend is hosted separately from backend
# If not set, frontend will use same-origin "/api"
# Examples:
#   VITE_API_BASE_URL=https://api.yourdomain.com
#   VITE_API_BASE_URL=https://api.yourdomain.com/api
# VITE_API_BASE_URL=

# ===== NODE ENVIRONMENT =====
# Set to "production" for production builds
# Set to "development" for development (default)
# NODE_ENV=development
